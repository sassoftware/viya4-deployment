---
- name: Add csi drivers
  command: kubectl apply --kubeconfig {{ kubeconfig }} -k "github.com/kubernetes-sigs/aws-efs-csi-driver/deploy/kubernetes/overlays/stable/?ref=master"
  when:
    - V4_CFG_MANAGE_STORAGE
    - PROVIDER == "aws"
  tags:
    - install
    - uninstall
    - upgrade