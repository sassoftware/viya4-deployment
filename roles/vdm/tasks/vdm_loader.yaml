- name: Add Overlays
  overlays:
    add: "{{ overlays }}"
    existing: "{{ vdm_overlays }}"
  register: new_overlays
  tags:
    - install
    - uninstall
    - upgrade

- set_fact:
    vdm_overlays: "{{ new_overlays.result }}"
  tags:
    - install
    - uninstall
    - upgrade