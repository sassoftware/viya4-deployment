- name: Check file existence {{ dest }}
  stat:
    path: "{{ dest }}"
  register: stat_result
  tags:
    - vdm

- name: Copy file {{ src }}
  copy:
    src: "{{ src }}"
    dest: "{{ dest }}"
    mode: "0600"
  when:
    - not stat_result.stat.exists
  tags:
    - vdm